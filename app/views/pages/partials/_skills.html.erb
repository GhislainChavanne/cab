<div class="pt-4 pb-0 lg:pb-12 col-span-4 grid grid-cols-1 md:grid-cols-2 gap-6 montserrat-light text-sm">
  <div>
    <p class="niramit-semibold lg:text-xs xl:text-sm mb-2"><%= I18n.translate('presentation.fields') %></p>
    <ul class="list-disc lg:text-xs xl:text-sm">
      <% I18n.translate('skills.firm.fields').each do |field| %>
        <li><%= field %></li>
      <% end %>
    </ul>
  </div>

  <div>
    <p class="niramit-semibold lg:text-xs xl:text-sm mb-2"><%= I18n.translate('presentation.stages') %></p>
    <ul class="list-disc lg:text-xs xl:text-sm">
      <% I18n.translate('skills.firm.stages').each do |stage| %>
        <li><%= stage %></li>
      <% end %>
    </ul>
  </div>
</div>